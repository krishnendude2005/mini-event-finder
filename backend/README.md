# Mini Event Backend

A minimal backend for event management, using Express and mock data.

## Setup

1. Install dependencies:
   ```
npm install
   ```
2. Start the server:
   ```
npm start
   ```
   or for development with live reload:
   ```
npm run dev
   ```

Server runs on http://localhost:4000 and provides `/api/events` endpoints.
